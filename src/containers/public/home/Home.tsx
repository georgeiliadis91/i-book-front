import React from 'react';
import Search from '../../../components/share/Search';

interface Props {}

const Home = (props: Props) => {
	return (
		<div>
			<h2>Home</h2>
			<Search />
		</div>
	);
};

export default Home;
