import React from 'react';
import './header.style.scss';
interface Props {}

const Header = (props: Props) => {
	return (
		<div id='header'>
			<h2>Header</h2>
		</div>
	);
};

export default Header;
